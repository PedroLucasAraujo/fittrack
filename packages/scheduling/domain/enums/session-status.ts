/**
 * Session lifecycle status.
 *
 * A Session is a sellable time unit defined by a professional.
 * It can be active (available for booking) or archived (no longer bookable).
 */
export enum SessionStatus {
  ACTIVE = 'ACTIVE',
  ARCHIVED = 'ARCHIVED',
}
